<section class="hero is-primary">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
      回答編集
      </h1>
    </div>
  </div>
</section>

<% if @programmer.errors.any? %>
<div class="notification is-danger">
    <h2><%= pluralize(@programmer.errors.count, "error") %> prohibited this object from being saved: not successfully</h2>
    <ul>
      <% @programmer.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>


<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-6">
<%= form_for @programmer do |f| %>
<%= f.label :title, "言語名", class: "label" %>
<%= f.text_field :title, class: "input" %>

<%= f.label :body, "回答", class: "label" %>
<%= f.text_area :body, class: "textarea" %>

<%= f.label :image, "画像", class: "label" %>
<%= f.attachment_field :image, class: "input" %>

<%= f.submit '投稿', class: "button is-success"%>

    <% end %>
   </div>
  </div>
 </div>
</section>
